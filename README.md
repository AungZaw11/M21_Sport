⚛️ React + Vite Starter
This template offers a minimal yet powerful setup to kickstart your React project using Vite. It includes hot module replacement (HMR) for a fast development experience and a basic ESLint configuration to help you write clean, consistent code.
🔌 React Plugin Options
Choose one of the official Vite plugins to enable Fast Refresh:

- — uses Babel
- — uses SWC
  ⚙️ React Compiler (Optional)
  This template does not include the new React Compiler by default. If you'd like to enable it, follow the official installation guide.
  🧹 ESLint Configuration
  The included ESLint setup provides basic linting rules for React development. For production-grade applications, we recommend:
- Switching to TypeScript for type safety
- Enabling type-aware linting with
  Explore the React + TypeScript Vite template for a ready-to-use setup.
